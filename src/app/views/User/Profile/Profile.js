import { Layout } from '../../../components/common'

const Profile = ({ props }) => {
    return (
        <Layout categories={false}>
            <div>Profile</div>
        </Layout>
    )
}
export default Profile
